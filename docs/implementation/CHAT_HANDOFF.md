# SakhiCircle Cross-Chat Handoff

## Current state

- Last completed task: `SC-700` — the revised Gate G7 analytics, retention, durable delivery, deployment, quota, and budget contract was approved on 2026-08-28.
- Current task: none. `SC-705` is `READY` and has not started.
- Current blockers: none for starting SC-705 planning. Before resource creation, SC-705 must show the proposed project ID, billing account, owners, and commands for explicit confirmation. Live Gemini/deployed evidence remains scheduled through `SC-725`.
- Web implementation: `apps/web` is the primary React/Vite/PWA client. `apps/mobile` and native evidence are frozen historical references.
- Backend feature implementation: `/health` and `/ready` remain stable; protected production requests require verified Firebase ID and App Check tokens; confirmed structured onboarding fields can be saved through `PUT /api/v1/profile`; authenticated journey draft and confirmation endpoints enforce the strict G5 contract; the consent-gated read-only recommendation endpoint enforces G6 with deterministic synthetic data; the FastAPI regression suite passes 55/55 as last recorded by SC-600.

## Start a new chat

Copy this prompt and replace only the task ID:

> Work only on task `SC-XXX` from `docs/implementation/WORKBOARD.md`. First read `AGENTS.md`, `PRODUCT.md`, `DESIGN.md`, `SakhiCircle Technical Requirements.md`, `docs/validation/acceptance.feature`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`. Check that all dependencies and approval gates are satisfied. Follow the locked test-first rule: add the relevant test, run it red for the missing behavior, implement only the named task, run focused and required suites, update the workboard and handoff, show evidence, and stop. Do not begin the next task.

## Required handoff entry

Every task chat appends one entry below:

```text
### YYYY-MM-DD — SC-XXX
- Outcome:
- Files changed:
- Decision made:
- Red test evidence:
- Green test/build evidence:
- UI/API evidence:
- Known limitation:
- Approval received or required:
- Exact next task:
```

## Evidence log

### 2026-08-20 — SC-010
- Outcome: Tonal Sage visual direction selected.
- Files changed: `DESIGN.md`, `docs/ui-options/README.md`.
- Decision made: true white plus tonal sage; no decorative warm accent.
- UI evidence: `docs/ui-options/15-tonal-sage.png` approved by the user.
- Approval received: Gate G1 approved.
- Exact next task: `SC-020`.

### 2026-08-20 — SC-020 (legacy prototype history)
- Outcome: approved UI contract locked before implementation of the original browser prototype.
- Files changed: `apps/web/src/test/App.test.tsx`, `apps/web/e2e/app-shell.spec.ts`.
- Red test evidence: 5 existing unit tests passed and 3 new Tonal Sage tests failed for missing UI; mobile responsive case passed and desktop rail case failed for missing rail behavior.
- UI implementation: none.
- Approval required: Gate G2 architecture review before `SC-100`.
- Exact next task: `SC-030`.

### 2026-08-21 — SC-000
- Outcome: cross-chat workboard, approval gates, architecture review, and handoff protocol created.
- Files changed: `docs/implementation/WORKBOARD.md`, `docs/implementation/ARCHITECTURE_REVIEW.md`, `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: one task ID per chat, test-first, stop after evidence.
- Approval required: Gate G2.
- Exact next task: `SC-030`.

### 2026-08-21 — SC-030 (superseded)
- Outcome: original browser/PWA architecture baseline A1–A5 approved as written; this baseline was later superseded by the native-only direction.
- Files changed: `docs/implementation/ARCHITECTURE_REVIEW.md`, `docs/implementation/WORKBOARD.md`, `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: proceed with the proposed repository, trust, data, AI/voice, integrations, analytics, local-first, and budget boundaries.
- Red test evidence: not applicable; approval-only governance task.
- Green test/build evidence: not applicable; no product code changed.
- UI/API evidence: architecture review document approved by the user.
- Known limitation: feature-specific schemas and screens still require their later approval gates.
- Approval received or required: the original Gate G2 was approved on 2026-08-21 and reopened when the user chose a native app.
- Exact next task at the time: `SC-100`.

### 2026-08-21 — SC-100 (legacy prototype history)
- Outcome: replaced the legacy magenta/teal theme in the original browser prototype with the approved OKLCH Tonal Sage semantic tokens, set its theme color to `#24483b`, added an original illustrated Sakhi guide with interactive quick help, and corrected responsive orientation/alignment.
- Files changed: `apps/web/src/App.tsx`, `apps/web/src/styles.css`, new `apps/web/src/assets/sakhi-guide.png`, `apps/web/index.html`, `apps/web/vite.config.ts`, generated `apps/web/vite.config.js`, `apps/web/src/test/App.test.tsx`, `apps/web/e2e/app-shell.spec.ts`, `docs/implementation/WORKBOARD.md`, `docs/implementation/CHAT_HANDOFF.md`, and refreshed `docs/ui-review/*.png` evidence.
- Decision made: use Sakhi Sage for primary actions and focus, Pale Sage for grouped/help/selected surfaces, Rooted Ink for text, and reserve Mid Sage as a non-text token. The sign-in screen uses a dignified older Indian guide as a reusable help entry point, keeps the form centered on desktop, and preserves the authenticated rail at the left edge.
- Red test evidence: the 2 token/theme tests initially failed; responsive revision tests then failed for missing concise copy and left-edge rail behavior; the guide contract failed before implementation because its copy, image, and accessible help interaction were absent.
- Green test/build evidence: the focused guide unit test passed; focused guide browser tests passed 2/2; the full legacy prototype browser suite passed 12/12; its production TypeScript/Vite PWA build passed; backend suite passed 3/3. The legacy prototype unit suite passed 7/9, with only the two locked SC-110 Today-content preview tests still red.
- UI/API evidence: visual captures confirm the 360×800 mobile sign-in has no horizontal or vertical overflow and the guide crop is readable; the 1280px desktop sign-in stays centered with the guide on the left. The guide exposes three keyboard-accessible quick-help topics and a live response. Refreshed screenshots are in `docs/ui-review/`; no API code changed.
- Known limitation: the Sakhi guide currently provides deterministic local quick help rather than a networked AI chatbot. The approved Today hierarchy, voice/text actions, circle preview, and mentor preview remain intentionally unimplemented for SC-110.
- Approval received or required: superseded before approval by the user's native-only direction.
- Exact next task: `SC-040` documentation pivot, followed by native architecture approval in `SC-050`.

### 2026-08-21 — SC-040
- Outcome: converted the current SakhiCircle documentation from a responsive browser/PWA target to a native Android and iOS app target.
- Files changed: `PRODUCT.md`, `DESIGN.md`, `SakhiCircle Technical Requirements.md`, `README.md`, `AGENTS.md`, `docs/validation/acceptance.feature`, `docs/validation/VALIDATION_CONTRACT.md`, `docs/ui-options/README.md`, `docs/implementation/ARCHITECTURE_REVIEW.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: use React Native, TypeScript, Expo Router, Expo development builds, and EAS Build in `apps/mobile`; retain `apps/web` as a legacy visual prototype only. Use native Firebase modules and App Check, with a Firebase Hosting domain only for passwordless auth links.
- Red test evidence: not applicable; documentation and architecture change only.
- Green test/build evidence: documentation terminology audit completed. No executable tests were run because product code did not change; the locked acceptance wording was converted from browser measurements to native device equivalents under the user's explicit platform change.
- UI/API evidence: not applicable; native implementation has not started and the FastAPI boundary remains unchanged.
- Known limitation: planned mobile commands remain non-runnable until `SC-105` scaffolds `apps/mobile`.
- Approval received or required: user directed the mobile-only product pivot; revised A1–A5 architecture decisions require Gate G2 approval.
- Exact next task: `SC-050` — review and approve the revised native architecture baseline.

### 2026-08-21 — SC-050 through SC-130 (user-directed migration batch)
- Outcome: approved Gate G2, created the Expo SDK 56 portrait-only application, implemented the no-scroll bilingual sign-in and deterministic Sakhi guide, added demo authentication state, and built the authenticated Today/My Circle/Mentors shell with exactly three bottom tabs.
- Files changed: `apps/mobile/**`, root workspace scripts and lockfile, `README.md`, `docs/implementation/ARCHITECTURE_REVIEW.md`, `docs/implementation/WORKBOARD.md`, `docs/implementation/CHAT_HANDOFF.md`, and `docs/validation/evidence/native/**`.
- Decision made: keep FastAPI unchanged, use small React contexts/adapters, keep real Firebase/App Check behind G4, and retain the frozen web prototype until Android and iOS gates pass and SC-140 is approved.
- Red test evidence: the reduced-motion test first failed because its hook was missing; the large-text contract first failed because its accessibility scale token was missing.
- Green test/build evidence: React Native Testing Library 3/3 suites and 14/14 tests; TypeScript pass; Android JavaScript export pass; Android Gradle build pass with 511 tasks; FastAPI 3/3 pass; CocoaPods installed 107 iOS pods; Expo Doctor 20/21 with the SDK 56 Hermes V1 known regression as the only warning.
- UI/API evidence: Android 360×800 captures cover sign-in, keyboard compaction, portrait lock, Sakhi help, Today, Hindi, and 200% text/reduced motion in `docs/validation/evidence/native/`. No FastAPI endpoint or response schema changed.
- Known limitation: iOS compilation and screenshots require full Xcode; Maestro failed three host/device attempts and was stopped under the three-fix rule; TalkBack, VoiceOver, external-keyboard, and preview-build overlay checks remain. Frozen web tests pass 7/9 with two stale locked prototype expectations.
- Approval received or required: Gate G2 was approved. Gate G3/SC-140 is still required and web retirement is not authorized.
- Exact next task: finish SC-130 after installing full Xcode and resolving the Maestro host driver, then request SC-140 approval.

### 2026-08-21 — SC-060 (Android-only revision)
- Outcome: converted the native client contract from Android/iOS to Android-only React Native and removed the iOS artifacts installed or generated for this project.
- Files changed: `apps/mobile/app.json`, `apps/mobile/package.json`, `apps/mobile/eas.json`, `apps/mobile/src/features/auth/SignInScreen.tsx`, `apps/mobile/src/test/app-shell.test.tsx`, root workspace scripts, active product/design/technical/validation documentation, and implementation records. The generated `apps/mobile/ios` directory was removed.
- Decision made: continue with Expo SDK 56 and React Native for Android only; retain FastAPI and the frozen web prototype. Xcode remains installed because the user installed it directly and it was outside this cleanup scope.
- Red test evidence: the focused native-shell contract failed because `app.json` still declared both Android and iOS.
- Green test/build evidence: the focused native-shell suite passes 7/7 Android-only assertions; the full mobile suite passes 3/3 suites and 14/14 tests; TypeScript, Android JavaScript export, Android Gradle build (511 tasks), and FastAPI 3/3 pass. Expo Doctor passes 20/21 checks with only the known Expo SDK 56 Hermes V1 warning.
- UI/API evidence: no product UI or FastAPI interface changed; existing Android evidence remains in `docs/validation/evidence/native/android/`.
- Known limitation: Maestro, TalkBack, and external-keyboard verification remain open under `SC-130`.
- Approval received or required: the user explicitly directed the Android-only conversion and iOS cleanup. Gate G3 remains required before web retirement.
- Exact next task: finish the Android verification gates for `SC-130`, then request `SC-140` approval.

### 2026-08-21 — SC-065 and SC-070 (form submission and website-first build plan)
- Outcome: recorded the completed Patchamomma idea-form checkpoint, restored `apps/web` as the active mobile-first PWA, narrowed the judged MVP to one data-driven vertical slice, and froze the Android implementation as historical evidence.
- Files changed: `PRODUCT.md`, `DESIGN.md`, `README.md`, `AGENTS.md`, `SakhiCircle Idea.md`, `SakhiCircle Technical Requirements.md`, `docs/validation/acceptance.feature`, `docs/validation/VALIDATION_CONTRACT.md`, `docs/ui-options/README.md`, `docs/validation/evidence/native/README.md`, `docs/implementation/ARCHITECTURE_REVIEW.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: build a locally runnable React/Vite PWA, deploy it primarily to Firebase Hosting, retain FastAPI for Cloud Run, use Firestore plus deterministic synthetic data, connect one bounded Gemini/ADK journey workflow only after the fallback passes, and defer payments/full circles/native distribution.
- Red test evidence: documentation audit found ten active Markdown files still declaring Android/native delivery as the current product.
- Green test/build evidence: eleven active Markdown files passed nonempty, final-newline, tab, local-link, current-platform, milestone, and current-task audits. The locked acceptance contract now contains the website/PWA, 360×800, local/cloud parity, deterministic fallback, and pseudonymous event scenarios. No product code changed.
- UI/API evidence: none; existing browser and Android screenshots are preserved, and new web evidence begins with `SC-081`.
- Known limitation: the existing web implementation still reflects earlier prototype assertions and must be reconciled test-first; Firebase emulators and a one-command local startup are not yet configured.
- Approval received or required: the user confirmed the website-first discussion by requesting these updates after submitting the form. G3 still requires approval of the implemented local responsive shell.
- Exact next task: `SC-080` — lock web shell, no-scroll mobile sign-in, and desktop alignment tests before changing `apps/web`.

### 2026-08-21 — SC-080
- Outcome: locked browser assertions for every required 360×800 sign-in action, the no-scroll viewport contract, the full-width bottom navigation below 900px, and desktop rail/content alignment.
- Files changed: `apps/web/e2e/app-shell.spec.ts`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: desktop alignment is measured against the canvas remaining to the right of a rail that touches the viewport edge; the permitted geometry tolerance is 1px at the edge and 2px at the content center.
- Red test evidence: focused Playwright run completed 16 checks across the configured mobile and desktop projects: 14 passed and 2 expected-red project executions identified the same missing behavior. At 1280×800 the rail starts at x=24px instead of x≤1px, and the main-content center is 117px away from the remaining-canvas center instead of ≤2px.
- Green test/build evidence: no implementation was authorized in this task. The new 360×800 required-action/no-scroll assertion and 899px bottom-bar assertion pass in both configured projects; existing accessibility target, keyboard, help, focus-transition, and overflow assertions also pass.
- UI/API evidence: no UI or API code changed. Browser geometry is captured in the Playwright error contexts and retained traces; visual correction belongs to `SC-081`.
- Known limitation: the focused suite intentionally remains red until `SC-081` removes the desktop rail inset and centers content in the remaining canvas. Full suites and screenshots are deferred until that implementation task.
- Approval received or required: the user approved `SC-080` on 2026-08-21. The locked assertions may not be weakened during implementation.
- Exact next task: `SC-081` — reconcile the existing web shell and make the focused browser contract green without weakening assertions.

### 2026-08-21 — SC-081
- Outcome: reconciled the responsive web shell so the desktop navigation rail touches the viewport edge and the main content remains centered in the canvas to its right, then applied the user-requested Olive Cream revision and a maroon Sakhi illustration while preserving the approved mobile sign-in and bottom-navigation behavior. Follow-up visual feedback keeps Sakhi's complete legs and shoes visible on desktop, anchors “Hi, let me help you” in a speech bubble at her head, and delivers a cleaner flagship polish pass across sign-in and the authenticated shell.
- Files changed: `DESIGN.md`, `apps/web/src/App.tsx`, `apps/web/src/styles.css`, `apps/web/src/test/App.test.tsx`, `apps/web/e2e/app-shell.spec.ts`, `apps/web/src/assets/sakhi-guide-maroon.png`, `apps/web/src/assets/sakhi-guide-maroon.webp`, `apps/web/index.html`, `apps/web/vite.config.ts`, `apps/web/scripts/capture-ui-review.mjs`, `docs/ui-review/mobile-login.png`, `docs/ui-review/mobile-shell.png`, `docs/ui-review/desktop-login.png`, `docs/ui-review/desktop-shell.png`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: desktop uses a fixed 210px edge rail plus a flexible remaining-canvas column; the main surface is capped at 900px with 24px side clearance and centered inside that column. After correcting the initially reversed mapping, the requested `#7A8450` replaces the green action role and `#F8EFDA` replaces the white canvas role. Because those colors have only 3.50:1 mutual contrast, accessible Rooted Ink `#10140B` remains the text/focus color. Sakhi's outfit is deep maroon with antique-gold detail; the generated source is preserved as PNG and the app consumes a 66KB WebP. The cream illustration panel is locked to `#F8EFDA`, with side-edge blending to remove the generator's raster backdrop seam. Desktop reserves 84px above a bottom-anchored, aspect-ratio-preserving portrait for a centered speech bubble; mobile keeps its original 0.9rem bubble font and upper-body crop. The polish pass replaces thick control outlines with 1px semantic borders, gives only the email action primary emphasis, makes demo access secondary, opens the three-step journey into breathable desktop columns, adds 180–240ms state motion with a reduced-motion fallback, and removes the non-functional duplicate “How this works” control. On viewports up to 780px, the optional three-step guide becomes a 48px-plus disclosure row, starts closed, and closes again whenever the viewport switches from desktop to mobile so the default 360×800 Today view has no page scrollbar.
- Red test evidence: the locked desktop geometry test failed in both Playwright projects before implementation: rail x was 24px instead of at most 1px, and the remaining-canvas centering delta was 117px instead of at most 2px. The requested palette test then failed against the old sage/white tokens and again before correcting the initially reversed cream/olive role mapping. The illustration-background contract failed while the guide panel still used the olive action token. The portrait test then measured the image bottom at 798px against a 747px panel boundary. The polish contract first failed because the email and demo actions shared the same olive background, then failed while the dead “How this works” control and stale help copy remained. The new mobile-shell regression measured 278px of default overflow before the optional steps were collapsed.
- Green test/build evidence: the focused desktop geometry run passed 2/2; focused shell unit tests passed 5/5; the corrected Olive Cream tests passed 2/2; focused guide/background tests passed 2/2; focused portrait/bubble and polish runs each passed 2/2; the focused no-scroll and responsive-label run passed 4/4; the complete browser suite passed 22/22; the production TypeScript/Vite PWA build passed with a 67.70KB optimized guide asset; the design detector returned no findings; and the FastAPI suite passed 3/3.
- UI/API evidence: refreshed Olive Cream 360×800 and 1280×800 login/shell captures are in `docs/ui-review/`. Visual and live interaction QA confirms the maroon outfit, seamless `#F8EFDA` illustration panel, complete desktop legs and shoes, head-anchored speech bubble, clearer sign-in hierarchy, mobile no-scroll sign-in, no-scroll default Today view with optional expandable guidance, full-width bottom navigation, desktop open three-step guidance, plus the desktop left-edge rail and centered content. The updated UI is open at `http://127.0.0.1:4174/`. No API interface changed.
- Known limitation: the complete unit run remains 7/9 because two pre-existing legacy Watercolour Today/preview assertions do not match the active onboarding-first shell; they were not weakened or expanded into SC-081. The active shell unit contract and all browser assertions pass.
- Approval received or required: the user approved SC-081 on 2026-08-21.
- Exact next task: `SC-082` — add one local command for the website, FastAPI, and deterministic adapters/emulators.

### 2026-08-21 — SC-082
- Outcome: added `pnpm dev:local`, which starts the React/Vite PWA and FastAPI together on `0.0.0.0`, selects the explicit deterministic adapter profile, enables the development-only synthetic demo, disables paid API calls, waits for both services, and shuts them down together.
- Files changed: `package.json`, `scripts/dev-local.mjs`, `scripts/tests/dev-local.test.mjs`, `apps/web/.env.example`, `apps/web/src/vite-env.d.ts`, `services/api/.env.example`, `services/api/app/config.py`, `services/api/app/main.py`, `services/api/tests/test_health.py`, `README.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: deterministic adapters are the zero-credential local default; the existing Firebase emulator ports remain reserved in `firebase.json` for later feature adapters. The orchestrator refuses to start without the project virtualenv, uses strict fixed ports, and verifies `/ready` reports `paidApiCallsEnabled: false` before announcing success.
- Red test evidence: the new root startup contract first failed because `dev:local` did not exist; the focused API readiness contract returned 404 before `/ready` was implemented.
- Green test/build evidence: startup contract passes 1/1; focused health/readiness passes 2/2; clean-start smoke reached Vite on port 5173 and FastAPI readiness on port 8080; complete Playwright passes 22/22; production web build passes; FastAPI passes 4/4.
- UI/API evidence: no product UI changed and all locked shell browser assertions remain green. `GET /health` still reports non-sensitive service state; new `GET /ready` reports development readiness, `adapterMode: deterministic`, and `paidApiCallsEnabled: false`. The smoke command left no listener on ports 5173 or 8080 after shutdown.
- Known limitation: the complete web unit suite remains at the pre-existing 7/9 baseline because the two legacy Today/Watercolour assertions still conflict with the approved onboarding-first shell. Firebase emulator process startup is not selected by default; current feature boundaries use the deterministic profile. LAN/phone access is intentionally deferred to `SC-083`.
- Approval received or required: the user approved `SC-082` on 2026-08-22; `SC-083` has not started.
- Exact next task: `SC-083` — verify the same local environment from `localhost` and a phone on the same Wi-Fi.

### 2026-08-22 — SC-083
- Outcome: verified the deterministic local environment through `localhost` and the machine's same-Wi-Fi address, completed demo entry on both routes, and received the user's confirmation that the physical phone reached the authenticated shell.
- Files changed: `apps/web/src/App.tsx`, `apps/web/src/test/App.test.tsx`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: keep Hindi's reviewed `नमस्ते, मीरा` greeting unchanged while applying the user-requested English greeting `Hi, Meera`; treat the DHCP-provided LAN address as session-specific evidence rather than committed configuration.
- Red test evidence: the focused greeting assertion failed because the English authenticated toolbar still rendered `Namaste, Meera` before the requested copy change.
- Green test/build evidence: the local startup contract passed 1/1; the focused greeting test passed 1/1; the complete browser suite passed 22/22; the production web build passed; and FastAPI passed 4/4. The complete web unit suite remained at its documented 7/9 baseline with only the two legacy Today/Watercolour assertions failing.
- UI/API evidence: `http://localhost:5173/` and `http://192.168.0.7:5173/` each loaded the sign-in screen and reached the authenticated Today shell through `Continue as Meera`; `/health` and `/ready` returned HTTP 200, readiness reported deterministic adapters with paid calls disabled, and the user confirmed the same flow passed on a physical phone with `Hi, Meera` visible.
- Known limitation: `192.168.0.7` is valid only for the current Wi-Fi assignment. The current demo entry is client-side; later phone flows that call FastAPI must replace the development API base of `localhost:8080` with a phone-reachable host.
- Approval received or required: the user approved `SC-083` on 2026-08-22.
- Exact next task: `SC-084` — present the local responsive shell evidence for Gate G3.

### 2026-08-22 — SC-084
- Outcome: presented refreshed mobile and desktop sign-in/authenticated-shell evidence together with the live one-command local environment, then fixed user-reported login language switching so the complete sign-in experience now changes between English and Hindi.
- Files changed: `apps/web/src/App.tsx`, `apps/web/src/test/App.test.tsx`, `apps/web/e2e/app-shell.spec.ts`, `apps/web/scripts/capture-ui-review.mjs`, `docs/ui-review/mobile-login.png`, `docs/ui-review/mobile-login-hi.png`, `docs/ui-review/mobile-shell.png`, `docs/ui-review/desktop-login.png`, `docs/ui-review/desktop-login-hi.png`, `docs/ui-review/desktop-shell.png`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: keep one typed English/Hindi login-copy contract covering visible copy, Sakhi help, accessible names, loading feedback, and Firebase status messages; store help/status state as semantic keys so already-visible feedback changes language immediately.
- Red test evidence: the focused bilingual-login test failed after clicking `हिंदी में देखें` because only the switch label changed while the login heading, fields, actions, and help remained English. The first duplicate startup attempt also found the existing SC-082 environment already occupying ports 5173 and 8080, so it was left running and verified rather than interrupted.
- Green test/build evidence: focused bilingual unit test passed 1/1; focused bilingual responsive browser test passed 2/2; complete Playwright suite passed 24/24; production TypeScript/Vite PWA build passed; FastAPI suite passed 4/4. The complete web unit suite now passes 8/10, with only the two stale Watercolour/Today preview assertions failing. Process inspection confirmed `node scripts/dev-local.mjs` owns the live Vite and Uvicorn services.
- UI/API evidence: refreshed English and Hindi captures show the complete sign-in experience at 360×800 and 1280×800; every required Hindi action remains visible without overflow, and `View in English` restores the English experience. The authenticated captures retain `Hi, Meera`. Live `http://127.0.0.1:5173/` returned HTTP 200; `/health` returned `status: ok`; `/ready` returned `status: ready`, `adapterMode: deterministic`, and `paidApiCallsEnabled: false`. SC-083 separately recorded and received approval for localhost and physical same-Wi-Fi phone demo entry.
- Known limitation: the two legacy Watercolour/Today unit assertions still conflict with the approved onboarding-first shell; all active shell unit assertions and browser checks pass. The same-Wi-Fi address is DHCP/session-specific; later phone flows that call FastAPI must use a phone-reachable API host instead of `localhost:8080`.
- Approval received or required: the user approved `SC-084` and Gate G3 on 2026-08-22.
- Exact next task: `SC-200` — present the web authentication/data/voice/privacy boundary for Gate G4.

### 2026-08-22 — SC-200
- Outcome: proposed the web authentication, ID-token, App Check, authorization, voice/transcript, stored-data, deletion, and privacy boundary for Gate G4 review.
- Files changed: `docs/implementation/G4_TRUST_BOUNDARY.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: approved as written. The boundary keeps unconfirmed transcripts in browser memory, persists only confirmed structured fields, stores no raw audio or verbatim transcript in the MVP, requires both Firebase ID token and App Check in production, routes operational writes through FastAPI, and makes account deletion authenticated, idempotent, and fail-closed.
- Red test evidence: not applicable; SC-200 is an approval-only architecture task and changes no executable behavior.
- Green test/build evidence: not applicable; no product code, API contract, rule, or approved assertion changed.
- UI/API evidence: current inspection confirms the API already rejects missing authentication, rejects the synthetic demo token in production, and keeps Firestore/Storage deny-by-default. App Check verification, client token forwarding, sign-out cleanup, transcript handling, and deletion remain explicitly unimplemented for later test-first tasks.
- Known limitation: the browser speech provider disclosure and the G7 analytics deletion key require implementation/schema evidence in their later tasks.
- Approval received or required: the user approved Gate G4 as written on 2026-08-22; `SC-200` is `DONE`.
- Exact next task: `SC-210` — implement deterministic demo and configured Firebase Auth/App Check states test-first.

### 2026-08-22 — SC-210
- Outcome: implemented observable deterministic and Firebase authentication sessions, production App Check token forwarding and API enforcement, explicit development-only demo entry, fail-closed production configuration, email-link completion cleanup, and bilingual sign-out with local user-state cleanup.
- Files changed: `apps/web/src/auth/runtime.ts`, `apps/web/src/auth/runtime.test.ts`, `apps/web/src/auth/firebase.ts`, `apps/web/src/App.tsx`, `apps/web/src/main.tsx`, `apps/web/src/styles.css`, `apps/web/src/test/App.test.tsx`, `apps/web/e2e/app-shell.spec.ts`, `apps/web/src/vite-env.d.ts`, `apps/web/.env.example`, `services/api/app/auth.py`, `services/api/app/config.py`, `services/api/tests/test_auth.py`, `services/api/.env.example`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: deterministic mode remains the zero-credential local default and issues the fixed synthetic token only after the explicit Meera action. Production startup requires Firebase web and App Check settings, protected request headers contain both the current ID token and App Check token, and the API validates the App Check token belongs to the configured Firebase app. Sign-out clears the pending email, draft, user query state, and confirmed-journey cache; a cleanup failure still ends the session and produces a retryable notice.
- Red test evidence: the new web contract first failed because `auth/runtime.ts` did not exist. API tests then failed because a valid ID token was accepted without App Check, the verifier was absent, and a production adapter could start outside the production environment.
- Green test/build evidence: focused auth runtime passes 4/4; focused sign-out UI passes 1/1; FastAPI passes 8/8; Playwright passes 26/26 across mobile and desktop; the TypeScript/Vite PWA production build passes. The full web unit suite passes 13/15, with only the same two pre-existing Watercolour/Today preview assertions failing against the approved onboarding-first shell.
- UI/API evidence: mobile and desktop browser tests enter through the explicit synthetic action, open the labelled profile menu, sign out, return to sign-in, and prove local draft/query values were removed. The API returns `401` for missing or copied demo authentication, `403` for missing or invalid production App Check, and `200` only when the test supplies both verified production tokens.
- Known limitation: configured Firebase and reCAPTCHA App Check are implemented and build-verified but cannot be exercised against a real Firebase project until deployment credentials and allowed domains are supplied. No paid API call was made.
- Approval received or required: the user approved `SC-210` on 2026-08-24; `SC-210` is `DONE` and `SC-300` is `READY`.
- Exact next task: `SC-300` — approve the English/Hindi speak-or-type onboarding fields and confirmation UX.

### 2026-08-24 — SC-300
- Outcome: presented and hardened one responsive two-stage English/Hindi onboarding design. The revision replaces a long nine-row ledger with a transcript check plus three grouped review sections, keeps one shared bilingual in-memory draft, and demonstrates empty, microphone-denied, unsupported, missing, changed, save-error, and success recovery states.
- Files changed: `docs/implementation/SC-300_ONBOARDING_UX.md`, `docs/ui-review/sc-300-onboarding-prototype.html`, `apps/web/scripts/capture-sc-300-review.mjs`, `scripts/tests/sc-300-design.test.mjs`, new `scripts/tests/sc-300-behavior.test.mjs`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`. Existing `docs/ui-review/sc-300-*.png` files predate the hardened revision and are not claimed as current evidence.
- Decision made: interface language changes labels and accessible names without replacing or translating learner-entered words; transcript review is explicit and collapsible; native field-specific editors replace generic editable text; focus moves to the active heading; plan consent is required and separate from optional matching consent; the privacy copy distinguishes SakhiCircle storage from possible speech-provider processing; and failed saves preserve the reviewed draft.
- Red test evidence: the original design-contract test first failed with `ENOENT`; the hardened revision contract then failed on the expected missing shared `onboardingDraft` behavior before implementation.
- Green test/build evidence: focused SC-300 design and behavior tests pass 14/14, covering shared and pending bilingual state, all seven recovery scenarios, transcript disposal, focus, exclusive disclosure, constrained inputs, consent gating, every changed row including optional city, and unique IDs. Inline prototype JavaScript parses; the Impeccable detector reports `[]`; independent post-fix review reports P0/P1/P2 all zero and improves the design-health trend from 20/40 to 36/40; complete Playwright passes 26/26; the production web build passes; and FastAPI passes 8/8. The unchanged complete web unit suite remains at 13/15 with only the same two pre-existing legacy Today/Watercolour assertions failing.
- UI/API evidence: the standalone prototype exposes English/Hindi capture and grouped review plus seven deterministic scenario queries. The updated capture script is prepared to produce eight core and seven state captures, run serious/critical axe checks, verify 48px targets, and verify 320px/200% reflow. The in-app browser safety policy blocked local `file://` inspection, so regenerated visual evidence and that script's browser assertions were not claimed. No app or API behavior changed.
- Known limitation: microphone access, transcript extraction, API persistence, and plan generation remain design-preview simulations for later test-first implementation. Regenerated screenshots still require running the approved local capture command outside the policy-blocked in-app browser surface.
- Approval received or required: the user approved the field set, English/Hindi copy, responsive layout, consent wording, and edit-before-save persistence boundary on 2026-08-24. `SC-300` is `DONE` and `SC-310` is `READY` but not started.
- Exact next task: `SC-310` — implement the approved text-first onboarding flow with a deterministic voice transcript adapter, test-first, when explicitly requested.

### 2026-08-24 — SC-310
- Outcome: implemented the approved two-stage English/Hindi text-first onboarding flow, deterministic local voice transcript adapter, editable structured review, explicit plan and optional matching consent, retry-safe authenticated profile save, and transcript disposal after success.
- Files changed: `apps/web/src/App.tsx`, `apps/web/src/main.tsx`, `apps/web/src/styles.css`, `apps/web/src/vite-env.d.ts`, `apps/web/src/onboarding/OnboardingFlow.tsx`, `apps/web/src/onboarding/OnboardingFlow.test.tsx`, `apps/web/src/onboarding/runtime.ts`, `apps/web/src/onboarding/runtime.test.ts`, `apps/web/e2e/app-shell.spec.ts`, `services/api/app/main.py`, `services/api/app/profile.py`, `services/api/tests/test_profile.py`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: the zero-credential voice adapter returns only a fixed synthetic transcript plus its visible local-provider disclosure; it exposes no audio value. Draft transcript and fields remain React memory only before confirmation. The browser derives the API host from the current page host unless `VITE_API_BASE_URL` is explicit, so same-Wi-Fi phone sessions can reach port 8080. The API accepts only reviewed structured fields, rejects extra transcript/raw-audio keys, requires authentication and affirmative plan consent, and keeps an in-memory deterministic profile store for this slice.
- Red test evidence: focused web tests initially failed because the onboarding runtime and component did not exist; focused API tests returned 404 because `PUT /api/v1/profile` did not exist.
- Green test/build evidence: focused onboarding runtime/UI passes 8/8, including serious/critical axe checks; FastAPI passes 11/11; the production TypeScript/Vite PWA build passes; complete Playwright passes 30/30 across 360×800 and 1280×800. The complete web unit suite is 20/22 with only the same two documented pre-existing Today/Watercolour preview assertions failing.
- UI/API evidence: live in-app browser review confirmed the responsive capture and grouped review screens at 360×800 and 1280×800; a 320px reflow check measured `scrollWidth` equal to `clientWidth` with 18px body text. Browser tests prove no profile request or local/session storage write occurs before confirmation, English/Hindi switching preserves learner words, deterministic voice remains editable, and the confirmed request contains no transcript or raw-audio field. `PUT /api/v1/profile` returns 401 without authentication, 422 for transcript/raw-audio/false-consent payloads, and 200 for the reviewed structured profile.
- Known limitation: SC-310 intentionally implements the deterministic local voice adapter, not a production browser speech provider; real microphone/provider policy integration remains separately scoped. The Impeccable detector reports one advisory pre-existing 6px profile-menu radius outside the documented scale and no new SC-310-specific finding.
- Approval received or required: the user approved `SC-310` on 2026-08-25; `SC-310` is `DONE` and `SC-400` is `READY`.
- Exact next task: `SC-400` — present the versioned 28-day journey schema and fallback contract for Gate G5 when explicitly requested.

### 2026-08-25 — SC-400
- Outcome: approved and locked the strict `1.0.0` bilingual 28-day journey schema, deterministic safety/accessibility review rules, bounded retry policy, curated fallback behavior, and edit-before-persistence boundary for Gate G5.
- Files changed: `docs/implementation/G5_JOURNEY_CONTRACT.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: approved as written. The contract uses one strict schema for deterministic fixtures, Gemini/ADK, and fallback; requires exactly four seven-day weeks with English/Hindi content and confirmed-availability limits; permits one full retry for invalid workflow output; immediately falls back when the configured provider is unavailable; and persists only an explicitly confirmed edited journey.
- Red test evidence: not applicable; SC-400 is an approval-only contract task and changes no executable product behavior.
- Green test/build evidence: not applicable; no product code, API implementation, or approved assertion changed. Documentation consistency was inspected against the product, technical, acceptance, G4, architecture, workboard, and SC-310 contracts.
- UI/API evidence: the proposal fixes the planned API boundary as an authenticated unsaved `POST /api/v1/journeys` draft followed by explicit `PUT /api/v1/journeys/{journeyId}` confirmation. It specifies the English/Hindi fallback notice and the exact learner-editable fields; neither endpoint nor UI is implemented in SC-400.
- Known limitation: real Gemini/ADK behavior is deferred to SC-600; SC-410 will implement only the deterministic fixture, fallback, editable confirmation, and persistence behavior when started.
- Approval received or required: the user approved Gate G5 as written on 2026-08-25; `SC-400` is `DONE` and `SC-410` is `READY`.
- Exact next task: `SC-410` — implement deterministic journey generation and editable confirmation test-first when explicitly requested.

### 2026-08-25 — SC-410
- Outcome: implemented authenticated deterministic journey generation, the strict bilingual `1.0.0` Pydantic contract, validated curated fallback construction, an editable four-week browser review, reject-without-write behavior, and explicit confirmation persistence.
- Files changed: `services/api/app/journey.py`, `services/api/app/main.py`, `services/api/tests/test_journey.py`, `apps/web/src/journey/runtime.ts`, `apps/web/src/journey/JourneyFlow.tsx`, `apps/web/src/journey/JourneyFlow.test.tsx`, `apps/web/src/App.tsx`, `apps/web/src/main.tsx`, `apps/web/src/onboarding/OnboardingFlow.tsx`, `apps/web/src/styles.css`, `apps/web/e2e/app-shell.spec.ts`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: `POST /api/v1/journeys` reads only the authenticated learner's confirmed profile and returns an unsaved deterministic draft; it never writes a journey. The learner explicitly opens the draft after profile confirmation, may switch English/Hindi without regeneration, edit the start date/title/activity title/duration/instructions/alternative/reflection, or reject it. Only `PUT /api/v1/journeys/{journeyId}` validates the edited draft against the confirmed availability and stores a server-owned confirmed record plus trusted uid/confirmation-time metadata.
- Red test evidence: the focused backend suite first stopped at collection with `ModuleNotFoundError: app.journey`; the focused web suite first failed import resolution because `JourneyFlow.tsx` did not exist. These were the intended missing-behavior failures.
- Green test/build evidence: focused journey API passes 6/6; focused journey/onboarding UI passes 10/10 including serious/critical axe checks; focused responsive browser journey passes 2/2; complete FastAPI passes 17/17; complete Playwright passes 32/32 across mobile and desktop; and the TypeScript/Vite PWA production build passes. The complete web unit suite is 26/28, with only the same two documented pre-existing legacy Today/Watercolour preview assertions failing; no approved assertion was deleted, skipped, or weakened. Ruff passes on all SC-410 backend files.
- UI/API evidence: the 360×800 and 1280×800 browser cases prove no journey request before the post-profile action, one draft request despite React Strict Mode, no confirmation write during English/Hindi switching or editing, reject clearing the draft without a PUT, and one final PUT containing the edited title, duration, and instruction without uid, city, transcript, or raw audio. API tests prove 401 authentication enforcement, 409 without a confirmed profile, exact four-week/seven-day/consecutive-date structure, schedule limits, recursive unknown-field rejection, deterministic and fallback provenance, no state write after generation, and confirmed-only storage. In-app visual inspection was attempted after the Browser skill review, but the in-app browser blocked both local hostnames; Playwright responsive traces and DOM assertions provide the UI evidence instead.
- Known limitation: SC-410 intentionally makes zero Gemini/ADK calls; the real bounded workflow remains SC-600. The local deterministic API stores confirmed records in its process-local adapter; the production Firestore adapter remains part of the later configured cloud/deployment work. The Impeccable detector reports only the previously documented 6px profile-menu radius advisory, outside SC-410.
- Approval received or required: the user approved `SC-410` on 2026-08-26. `SC-410` is `DONE`; `SC-500` is `READY` and has not started.
- Exact next task: `SC-500` — approve matching filters, weights, and explanation contract for G6 when explicitly requested.

### 2026-08-26 — SC-500
- Outcome: proposed a strict deterministic matching and synthetic-data contract for Gate G6, including request consent, hard filters, a 65/100 threshold, stable scoring and tie-breaking, exactly three strongest reasons, bilingual ranking invariance, private empty states, and deterministic dataset boundaries.
- Files changed: `docs/implementation/G6_MATCHING_CONTRACT.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: proposed for approval. All hard filters run before a 100-point score weighted as hobby/goal 30, schedule 20, language 15, skill level 15, pace 10, format 10, and price 0 for the deferred-payment checkpoint. The endpoint returns at most three results at or above 65; the judged fixture leaves one synthetic top result. Each result shows its score and exactly three fixed bilingual reasons; accessibility, popularity, activity, age, and translation cannot affect ranking.
- Red test evidence: not applicable; SC-500 is an approval-only contract task and changes no executable product behavior.
- Green test/build evidence: not applicable; no product code, API implementation, or approved assertion changed. Documentation consistency was checked against the product, design, technical, acceptance, G4, G5, workboard, current profile fields, and existing API boundary.
- UI/API evidence: the proposal fixes `GET /api/v1/recommendations?type=partner|mentor` as an authenticated, consent-gated, read-only server-derived request. It defines the strict response, visible 100-point score and three reasons, deterministic bilingual copy, synthetic label, consent-off/no-match/error states, focus and reflow behavior, and forbidden response fields; none is implemented in SC-500.
- Known limitation: the checkpoint availability field describes practice capacity rather than specific days or clock times, so the contract labels the filter honestly as capacity overlap. Price remains inactive until a learner budget and payments receive a new approval. Recommendation decisions, contact exchange, messaging, analytics, and production marketplace behavior remain deferred.
- Approval received or required: the user approved Gate G6 as written on 2026-08-26. `SC-500` is `DONE`, and `SC-510` is `READY` but has not started.
- Exact next task: `SC-510` — generate deterministic synthetic data and implement one explainable recommendation test-first when explicitly requested.

### 2026-08-26 — SC-510
- Outcome: generated the versioned deterministic synthetic matching dataset and implemented the authenticated, consent-gated, read-only partner/mentor recommendation API plus one explicit, explainable post-journey partner recommendation in English and Hindi.
- Files changed: `services/api/app/synthetic_data.py`, `services/api/app/matching.py`, `services/api/app/main.py`, `services/api/tests/test_synthetic_data.py`, `services/api/tests/test_matching.py`, `services/api/tests/test_recommendations.py`, `apps/web/src/recommendation/runtime.ts`, `apps/web/src/recommendation/runtime.test.ts`, `apps/web/src/recommendation/RecommendationFlow.tsx`, `apps/web/src/recommendation/RecommendationFlow.test.tsx`, `apps/web/src/journey/JourneyFlow.tsx`, `apps/web/src/App.tsx`, `apps/web/src/main.tsx`, `apps/web/src/styles.css`, `apps/web/e2e/app-shell.spec.ts`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: the non-production API creates the source dataset from one committed seed and generator version, while the production adapter remains absent and fails closed instead of substituting demo data. Matching canonicalizes only reviewed categorical profile fields, applies all private hard filters before the locked 100-point score, and returns the stable top three at or above 65; the judged fixture returns one 100-point `Kavita Demo` partner after excluding the self, blocked, rejected, and incompatible fixtures. The web requests matching only after the confirmed journey and the labelled Find action, keeps the response in memory, selects returned reviewed bilingual copy without refetching, and clears it when consent becomes false.
- Red test evidence: the first focused backend run stopped during collection with `ModuleNotFoundError` for the expected missing `app.synthetic_data` and `app.matching` modules. The first focused web run stopped import resolution for the expected missing recommendation runtime and component.
- Green test/build evidence: focused synthetic/matching/recommendation backend passes 27/27; focused recommendation/journey web passes 10/10 including serious/critical axe checks; focused mobile/desktop recommendation browser passes 2/2; complete FastAPI passes 44/44; complete Playwright passes 34/34 across mobile and desktop; the TypeScript/Vite PWA production build passes; and Ruff passes on all SC-510 backend files. The complete web unit suite passes 31/33, with only the same two documented pre-existing legacy Today/Watercolour preview assertions failing; no approved assertion was deleted, skipped, or weakened.
- UI/API evidence: browser tests prove no recommendation request occurs before the explicit action, exactly one request returns one synthetic result with score plus three reasons, Hindi switching makes no second request, focus moves to the result heading, visible match controls stay at least 48px, and the surface has no horizontal overflow at 360px, 320px, or 200% text scale. API tests prove authentication/profile/consent preconditions, every partner and mentor hard filter, exact weights and 65 threshold, stable tie-breaking and maximum three results, deterministic counts of 250 learners/40 mentors/15 hobbies/25 deferred circles/90 activity days, byte repeatability, private empty/error states, forbidden response-field exclusion, no writes, zero AI/paid calls, and fail-closed production configuration.
- Known limitation: the production recommendation catalog adapter is intentionally not configured in SC-510 and returns an actionable 503; production Firestore data remains later cloud/deployment work. Activity does not affect ranking, price stays 0/0, and Gemini is not called. The Impeccable detector reports only the previously documented advisory 6px profile-menu radius outside the design scale, unrelated to SC-510.
- Approval received or required: the user approved `SC-510` on 2026-08-26. `SC-510` is `DONE`; `SC-590` is `READY` and has not started.
- Exact next task: `SC-590` — capture August 28 local and deployed vertical-slice evidence when explicitly requested.

### 2026-08-26 — SC-590
- Outcome: captured and approved the complete August 28 deterministic local vertical slice in the real browser at mobile and desktop sizes, including English/Hindi sign-in, synthetic voice transcript confirmation, the editable 28-day journey, explicit save, and one explainable synthetic match. The user later approved moving deployed capture to `SC-725` after its cloud prerequisites.
- Files changed: `docs/validation/evidence/web/sc-590/README.md`, six local browser captures in `docs/validation/evidence/web/sc-590/`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`. No product code changed.
- Decision made: preserve truthful evidence boundaries by closing `SC-590` on its approved local evidence and moving cloud proof to `SC-725`. The approved sequence is `SC-600` → `SC-700`/G7 → `SC-705` cloud foundation → `SC-710` analytics → `SC-715` persistent Firestore adapters → `SC-720` preview/live deployment → `SC-725` deployed evidence.
- Red test evidence: not applicable because SC-590 is evidence capture for already implemented behavior and changes no executable product behavior.
- Green test/build evidence: local startup contract 1/1; focused web 14/14; focused API 33/33; focused browser 4/4; complete FastAPI 44/44; complete Playwright 34/34 across mobile and desktop; production TypeScript/Vite PWA build passed. The complete web unit suite remains 31/33 only because the same two documented legacy Today/Watercolour preview assertions fail; no approved assertion was deleted, skipped, weakened, or rewritten.
- UI/API evidence: the current local server completed sign-in → deterministic voice/transcript review → confirmed profile → generated 28-day bilingual journey → edited title → confirmed journey → explicit partner request → Kavita Demo at 100/100 with exactly three reasons. Mobile sign-in measured zero overflow at 360×800; desktop navigation measured x=0 and the result had zero horizontal overflow. `/ready` reported deterministic adapters with paid calls disabled. A stale August 25 API process initially returned 404 for the journey endpoint; after verifying and replacing only the stale SakhiCircle processes, the current-source path passed.
- Known limitation: no Firebase project binding, Hosting URL, Cloud Run URL, Firebase CLI, or gcloud CLI exists in the workspace. Production also requires persistent Firestore adapters before deployment; these are now explicit later tasks rather than an SC-590 blocker.
- Approval received or required: the user completed and approved the full local English/Hindi journey and recommendation flow, then approved the corrected deployment-prerequisite plan on 2026-08-26. `SC-590` is `DONE`.
- Exact next task: `SC-600` — connect one bounded Gemini/ADK journey workflow behind the deterministic fallback when explicitly requested.

### 2026-08-26 — SC-600
- Outcome: connected one bounded three-stage Google ADK journey workflow behind the existing deterministic mode and curated fallback. The workflow runs plan generation, safety/accessibility review, then English/Hindi localization; validates the final result against the strict `1.0.0` journey contract; retries one invalid or rejected result; immediately falls back on timeout or provider unavailability; and leaves persistence behind the existing explicit confirmation endpoint.
- Files changed: `services/api/app/journey_workflow.py`, `services/api/app/journey.py`, `services/api/app/main.py`, `services/api/app/config.py`, `services/api/tests/test_journey_workflow.py`, `services/api/pyproject.toml`, `services/api/.env.example`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`. No frontend product code changed.
- Decision made: keep deterministic mode as the zero-credential default; construct the configured Gemini adapter only when production, paid-call opt-in, and an explicit secret are all present; send only the confirmed allowlisted profile fields; use an opaque server-derived journey ID rather than uid; cap each attempt at 10 seconds and the workflow at two attempts; cap ADK at three model calls per attempt; disable model-content telemetry; and pin Google ADK to the supported 2.x line because the locked G5 contract requires exact sequential shared-state stages.
- Red test evidence: the new focused backend contract first stopped at collection with `ModuleNotFoundError: No module named 'app.journey_workflow'`, proving the expected missing workflow boundary before implementation.
- Green test/build evidence: focused journey/workflow/readiness API passes 19/19; complete FastAPI passes 55/55; Ruff passes on every changed backend source and test; Python dependency validation reports no broken requirements; focused journey UI passes 5/5; complete Playwright passes 34/34 across mobile and desktop; and the production TypeScript/Vite PWA build passes. The unchanged complete web unit suite remains 31/33 only because the same two documented legacy Today/Watercolour preview assertions fail; no approved assertion was deleted, skipped, weakened, or rewritten.
- UI/API evidence: API tests prove first-pass `gemini_adk` provenance, one retry after invalid output, reviewer and localization rejection fallbacks, timeout and unavailability fallbacks, strict English/Hindi `1.0.0` output, exact allowlisted workflow input, server-owned identity/date fields, missing-production-configuration 503 behavior, zero external calls in deterministic mode, and zero-network construction of the real three-agent ADK graph. Existing responsive browser tests prove the returned bilingual draft stays unsaved until explicit confirmation and passes mobile/desktop interaction checks; no UI change was needed.
- Known limitation: no live Gemini request was executed because the workspace contains no `SAKHI_GEMINI_API_KEY`, and secrets must not be committed. The adapter is integration-tested with deterministic scripted workflows plus a zero-network construction test; a live production smoke test remains part of the later configured cloud/deployment evidence. Google ADK 2.7 currently deprecates `SequentialAgent` in favor of its newer workflow graph API, so the dependency is bounded below major version 3 while this approved sequential contract remains in force.
- Approval received or required: the user approved `SC-600` on 2026-08-27. `SC-600` is `DONE`; `SC-700` is `READY` and has not started.
- Exact next task: `SC-700` — approve the pseudonymous analytics schema and cloud-cost controls for G7 when explicitly requested.

### 2026-08-27 — SC-700
- Outcome: proposed the strict `analytics-v1.0.0` four-event allowlist, HMAC pseudonym/deletion boundary, at-most-once delivery contract, 90-day BigQuery retention, aggregate-only Looker proof, Mumbai deployment boundary, and preventive cloud-cost controls for Gate G7.
- Files changed: `docs/implementation/G7_ANALYTICS_COST_CONTRACT.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: proposed for approval. Production accepts only successful `profile_confirmed`, `journey_draft_created`, `journey_confirmed`, and `recommendation_presented` outcomes with exact categorical properties. FastAPI owns timestamps and versioned HMAC subject/event keys; Firestore receipts plus parameterized BigQuery writes enforce idempotency. Raw rows expire after 90 days and Looker sees aggregate counts/rates only. Cost controls lock Cloud Run to 0–2 small instances, cap Gemini at 3 workflows per subject/24 hours and 20/project day, bound BigQuery queries, and alert at USD 10/25/40.
- Red test evidence: not applicable; SC-700 is an approval-only contract task and changes no executable product behavior.
- Green test/build evidence: not applicable; no product code, API implementation, cloud resource, paid service, or approved assertion changed. Documentation consistency was checked against PRODUCT, DESIGN, the technical requirements, locked acceptance, G2, G4, G5, G6, the current API schemas, workboard, and handoff.
- UI/API evidence: the proposal fixes the future authenticated `POST /api/v1/analytics/events` request/response, exact row/view columns, success-only emission points, retry/conflict behavior, deletion resolution, failure codes, quota responses, IAM roles, and dashboard metrics. None is implemented or provisioned in SC-700.
- Known limitation: budget alerts and Cloud Run maximum instances are safeguards rather than absolute spending caps, so persistent application rate counters and BigQuery query limits are mandatory. A live event, aggregate view, deletion fixture, Firebase/Cloud project binding, and alert evidence remain SC-705/SC-710/SC-725 work.
- Approval received or required: Gate G7 approval is required. `SC-700` and G7 are `AWAITING APPROVAL`; `SC-705` remains `PENDING`.
- Exact next task: approve or revise Gate G7. After approval only, start `SC-705` as a separate provisioning task.

### 2026-08-28 — SC-700 review revision
- Outcome: resolved all five Gate G7 review findings without implementing product behavior or creating cloud resources. Analytics occurrences are now derived from successful server results and bound to signed receipts; a bounded OIDC-authenticated Cloud Tasks outbox survives client closure; the aggregate authorized view is isolated in `sakhi_reporting`; application dates and daily counters use `Asia/Kolkata`; and cost-control implementation/runtime verification have explicit task owners.
- Files changed: `docs/implementation/G7_ANALYTICS_COST_CONTRACT.md`, `docs/implementation/WORKBOARD.md`, and `docs/implementation/CHAT_HANDOFF.md`.
- Decision made: the browser may submit only a server-issued receipt and cannot choose event names or outcomes. SC-705 provisions the two-dataset/queue/IAM/budget foundation, new test-first task SC-706 implements persistent quotas plus the confirmed operator controls, SC-710 implements signed-receipt/outbox/BigQuery/Looker behavior, and SC-720 verifies live Cloud Run runtime controls.
- Red test evidence: not applicable; this is an approval-only contract correction and changes no executable product behavior.
- Green test/build evidence: not applicable; no product code, API implementation, cloud resource, paid service, or approved assertion changed. A documentation contract check verifies server authority, durable delivery ownership, two-dataset isolation, India calendar boundaries, task dependencies, and unchanged G7 approval state.
- UI/API evidence: the revised proposal defines the future receipt-only analytics request, server-derived event contents, durable task states, reporting-only identity, operator control document, failure responses, and task-specific verification. None is implemented or provisioned in SC-700.
- Known limitation: the controls remain proposed rather than live. Cloud project binding, queue/dataset/IAM evidence, application quota tests, configured BigQuery delivery, Looker proof, and deployed runtime verification remain SC-705 through SC-720 work.
- Approval received or required: the user approved revised Gate G7 on 2026-08-28. `SC-700` and G7 are `DONE`; `SC-705` is `READY` and later tasks remain `PENDING`.
- Exact next task: `SC-705` — provision the approved Google Cloud/Firebase foundation and cost guardrails when explicitly requested, showing project/billing/owner/command targets before resource creation.
