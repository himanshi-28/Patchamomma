rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Part 1 is deny-by-default. Feature-specific reads are opened only with tests.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
